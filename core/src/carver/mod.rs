pub mod model;
pub mod texture;
pub mod voxelizer;
pub mod brick;
use std::path::Path;

use anyhow::Result;

pub fn generate_materials(client_jar: &Path, output_path: &Path) -> Result<()> {
  todo!()
}